{
  "name": "cloudflare-pages-ssr",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i ./src/style.css -o ./dist/style.css --minify",
    "build:ssr": "vite build",
    "build": "npm run build:css && npm run build:ssr",
    "preview": "npx wrangler pages dev dist"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
